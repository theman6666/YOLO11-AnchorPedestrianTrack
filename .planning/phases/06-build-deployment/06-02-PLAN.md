---
phase: 06-build-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/run/app.py
  - frontend-vue/vite.config.ts
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02

must_haves:
  truths:
    - "Running `npm run build` generates optimized dist/ directory"
    - "dist/index.html contains references to hashed asset files"
    - "Flask serves index.html from frontend-vue/dist/ at root route"
    - "Flask serves static assets (JS, CSS, favicon) from dist/ directory"
    - "API routes (/video_feed, /detect, /results) continue working"
    - "All non-API routes return index.html (SPA support)"
    - "Build assets are minified and hashed for cache busting"
  artifacts:
    - path: "frontend-vue/dist/index.html"
      provides: "SPA entry point with asset references"
      min_lines: 10
    - path: "frontend-vue/dist/assets/"
      provides: "Hashed JS and CSS bundles"
      contains: "index-*.js and index-*.css files"
    - path: "src/run/app.py"
      provides: "Flask routes for serving SPA and API"
      exports: ["index()", "serve_assets()", "serve_favicon()", "catch_all()"]
  key_links:
    - from: "npm run build"
      to: "frontend-vue/dist/"
      via: "Vite build process"
      pattern: "vite build command"
    - from: "Flask app.py routes"
      to: "frontend-vue/dist/ files"
      via: "send_from_directory() calls"
      pattern: "send_from_directory.*DIST_DIR"
    - from: "Browser request to /"
      to: "dist/index.html"
      via: "Flask index() route"
      pattern: "@app.route\(\"\/\"\)"
    - from: "Browser request to /assets/*"
      to: "dist/assets/*"
      via: "Flask serve_assets() route"
      pattern: "@app.route\(\"\/assets\/"
---

<objective>
Configure production build optimization and integrate Flask static file serving to deploy the Vue.js SPA alongside the backend.

Purpose: Enable Flask to serve the production-built Vue.js application from the dist/ directory while preserving all API endpoints, creating a single-server deployment.
Output: Optimized production build in dist/ and Flask routes configured to serve the SPA with static assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-build-deployment/06-CONTEXT.md
@.planning/phases/06-build-deployment/06-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-build-deployment/06-01-SUMMARY.md

# Reference files for implementation patterns
@frontend-vue/vite.config.ts
@src/run/app.py
@frontend-vue/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and document Vite build configuration</name>
  <files>
    - frontend-vue/vite.config.ts
    - frontend-vue/package.json
  </files>
  <read_first>
    - frontend-vue/vite.config.ts
    - frontend-vue/package.json
    - frontend-vue/tsconfig.json
  </read_first>
  <action>
Verify that the Vite configuration is ready for production builds without modifications.

From vite.config.ts, verify the following configuration is present:

1. **No base path override needed** — Current config has no `base` specified, which defaults to `'/'` for production
   - This generates absolute asset paths like `/assets/index-xyz.js`
   - Works correctly when Flask serves the SPA at root path (per D-01)
   - Reference RESEARCH.md section "Pattern 3: Vite Base Path Configuration"

2. **Plugins configured correctly**:
   - `vue()` plugin compiles Single File Components
   - `vueDevTools()` plugin excluded from production build automatically

3. **Path aliases for clean imports**:
   - `'@': fileURLToPath(new URL('./src', import.meta.url))`
   - Allows imports like `import Component from '@/components/Component.vue'`

From package.json, verify build scripts:
- `"build": "run-p type-check \"build-only {@}\" --"` — Runs type-check and build-only in parallel
- `"build-only": "vite build"` — Creates production build in dist/
- `"type-check": "vue-tsc --build"` — Validates TypeScript before build

**Do NOT make any changes to vite.config.ts or package.json** — the current configuration is already correct for production builds per D-13 and D-14:
- D-13: "启用 Vite 的默认优化（代码分割、压缩、tree-shaking）"
- D-14: "使用时间戳或哈希文件名进行缓存破坏（Vite 默认行为）"

This task is verification-only to confirm no configuration changes are needed.
  </action>
  <verify>
    <automated>
      # Verify vite.config.ts exists and has required plugins
      test -f frontend-vue/vite.config.ts && \
      grep -q "vue()" frontend-vue/vite.config.ts && \
      grep -q "vueDevTools()" frontend-vue/vite.config.ts && \
      # Verify package.json has build scripts
      grep -q '"build":' frontend-vue/package.json && \
      grep -q '"build-only": "vite build"' frontend-vue/package.json && \
      echo "✅ Vite build configuration verified"
    </automated>
  </verify>
  <done>
Vite configuration verified as production-ready:
- vue() plugin compiles SFCs
- vueDevTools() excluded from production automatically
- No base path override (defaults to '/' for Flask root serving)
- Build scripts: npm run build creates production dist/
- Default optimizations enabled: code splitting, minification, tree-shaking (per D-13)
- Asset hashing enabled for cache busting (per D-14)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create production build and verify output</name>
  <files>
    - frontend-vue/dist/index.html
    - frontend-vue/dist/assets/
  </files>
  <read_first>
    - frontend-vue/package.json
    - frontend-vue/.env.production
  </read_first>
  <action>
Generate the production build and verify the output structure.

**Step 1: Run production build**
Execute the following command from the project root:
```bash
cd frontend-vue && npm run build
```

This command:
- Runs type-check (vue-tsc) and build-only (vite build) in parallel per package.json
- Loads `.env.production` automatically (Vite default behavior per D-11)
- Creates `frontend-vue/dist/` directory with optimized assets
- Embeds `VITE_API_BASE_URL=http://localhost:5000` into the bundle

**Step 2: Verify dist/ structure**
After build completes, verify the following exists:

1. `dist/index.html` — Entry point with asset references
   - Should contain `<script src="/assets/index-*.js">` with hash
   - Should contain `<link rel="stylesheet" href="/assets/index-*.css">` with hash

2. `dist/assets/` directory — Hashed JS and CSS bundles
   - `index-*.js` — Application JavaScript with content hash
   - `index-*.css` — Application CSS with content hash
   - Hash changes when content changes (cache busting per D-14)

3. `dist/favicon.ico` (if exists in public/) or other static assets

**Step 3: Verify embedded API URL**
Check that the production build embedded the correct API base URL:
```bash
grep -o 'baseURL:"[^"]*"' frontend-vue/dist/assets/index-*.js | head -1
```
Expected output: `baseURL:"http://localhost:5000"`

This confirms the build used `.env.production` correctly.

Per D-01: "构建输出保持在 `frontend-vue/dist/` 目录"
Per D-13: "启用 Vite 的默认优化（代码分割、压缩、tree-shaking）"
  </action>
  <verify>
    <automated>
      # Change to frontend-vue directory and run build
      cd frontend-vue && npm run build && \
      # Verify dist/index.html exists
      test -f dist/index.html && \
      # Verify dist/assets directory exists
      test -d dist/assets && \
      # Verify JS bundle exists (with hash)
      ls dist/assets/index-*.js >/dev/null 2>&1 && \
      # Verify CSS bundle exists (with hash)
      ls dist/assets/index-*.css >/dev/null 2>&1 && \
      # Verify API URL is embedded correctly
      grep -q "localhost:5000" dist/assets/index-*.js && \
      echo "✅ Production build created and verified"
    </automated>
  </verify>
  <done>
Production build verified:
- dist/index.html exists with asset references
- dist/assets/ contains hashed JS and CSS bundles
- Embedded API URL: http://localhost:5000 (from .env.production)
- Assets are minified and optimized (Vite default per D-13)
- File hashes enable cache busting (per D-14)
- Build output in frontend-vue/dist/ as specified in D-01
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Flask SPA serving routes</name>
  <files>src/run/app.py</files>
  <read_first>
    - src/run/app.py
    - frontend-vue/dist/index.html
  </read_first>
  <action>
Add Flask routes to serve the Vue.js SPA from the dist/ directory while preserving existing API endpoints.

**Add the following imports at the top of app.py** (after existing imports):
```python
from pathlib import Path
```

**Add the following directory path constants** (after existing constants like `PORT = 5000`):
```python
# Frontend Vue.js SPA dist directory
DIST_DIR = PROJECT_ROOT / "frontend-vue" / "dist"
```

**Add the following new routes** (AFTER all existing API routes, BEFORE the `if __name__ == "__main__":` block):

```python
# ========== SPA Serving Routes ==========

# Serve SPA index.html at root
@app.route("/")
def serve_spa_index():
    """Serve the Vue.js Single Page Application entry point."""
    return send_from_directory(str(DIST_DIR), "index.html")


# Serve static assets (JS, CSS, fonts, etc.)
@app.route("/assets/<path:filename>")
def serve_assets(filename):
    """Serve static assets from the Vue.js build output."""
    return send_from_directory(str(DIST_DIR / "assets"), filename)


# Serve favicon
@app.route("/favicon.ico")
def serve_favicon():
    """Serve the favicon from the Vue.js build output."""
    return send_from_directory(str(DIST_DIR), "favicon.ico")


# Catch-all route for SPA client-side routing
@app.route("/<path:path>")
def catch_all(path):
    """
    Catch-all route for SPA support.
    Returns index.html for any non-API route to support client-side routing.
    API routes (/video_feed, /detect, /results) are defined above and take priority.
    """
    # Check if the request is for an API route that should have been handled earlier
    if path.startswith(('video_feed', 'detect', 'results')):
        return f"API route /{path} not found", 404

    # Return index.html for all other routes (SPA client-side routing)
    return send_from_directory(str(DIST_DIR), "index.html")
```

**Critical implementation details:**
1. **Route ordering is critical** — API routes (`/video_feed`, `/detect/*`, `/results/*`) are already defined in app.py (lines 108-188)
2. **New SPA routes go AFTER API routes** — This ensures API requests are handled correctly before reaching the catch-all
3. **send_from_directory** — Standard Flask function for serving static files (per D-07)
4. **Path to DIST_DIR** — Uses `PROJECT_ROOT / "frontend-vue" / "dist"` to locate the build output (per D-01)
5. **Catch-all route** — Returns index.html for non-API routes, supporting Vue Router if added later (per D-09)

Reference RESEARCH.md section "Pattern 2: Flask SPA Serving with Catch-All Route" for complete pattern explanation.
Reference D-07: "Flask 使用 `send_from_directory` 从 `frontend-vue/dist/` 服务静态文件"
Reference D-08: "API 路由保持 `/video_feed`, `/detect/image`, `/detect/video`, `/results` 前缀"
Reference D-09: "所有非 API 请求返回 `index.html`（支持 SPA 路由）"

**Verify existing API routes are preserved:**
- `/video_feed` (line 108) — Camera streaming
- `/detect/image` (line 122) — Image detection
- `/detect/video` (line 156) — Video detection
- `/results/<path:filename>` (line 186) — Result file serving

These routes must NOT be modified or moved.
  </action>
  <verify>
    <automated>
      # Verify Flask app has new routes
      grep -q "DIST_DIR = PROJECT_ROOT" src/run/app.py && \
      grep -q 'def serve_spa_index' src/run/app.py && \
      grep -q 'def serve_assets' src/run/app.py && \
      grep -q 'def serve_favicon' src/run/app.py && \
      grep -q 'def catch_all' src/run/app.py && \
      grep -q 'send_from_directory(str(DIST_DIR), "index.html")' src/run/app.py && \
      echo "✅ Flask SPA serving routes added"
    </automated>
  </verify>
  <done>
Flask app.py updated with SPA serving:
- DIST_DIR constant points to frontend-vue/dist/ (per D-01)
- serve_spa_index() route serves index.html at /
- serve_assets() route serves JS/CSS from /assets/
- serve_favicon() route serves favicon.ico
- catch_all() route returns index.html for non-API routes (per D-09)
- All existing API routes preserved and functional (per D-08)
- Route ordering correct: API routes before catch-all (prevents breaking API endpoints)
  </done>
</task>

<task type="auto">
  <name>Task 4: Test Flask + SPA integration</name>
  <files>
    - src/run/app.py
    - frontend-vue/dist/
  </files>
  <read_first>
    - src/run/app.py
    - frontend-vue/dist/index.html
  </read_first>
  <action>
Verify that Flask correctly serves the Vue.js SPA and all API endpoints work together.

**Step 1: Start Flask server**
Run the following command to start Flask with the new SPA routes:
```bash
cd D:/YOLO11-AnchorPedestrianTrack && python src/run/app.py
```

Expected output:
```
Web service started: http://127.0.0.1:5000
 * Running on http://0.0.0.0:5000
```

**Step 2: Verify SPA serving**
Open a browser and navigate to: `http://localhost:5000`

Expected behavior:
- Page loads and displays the Vue.js application
- Browser DevTools Network tab shows:
  - `GET /` → 200 → Returns index.html from dist/
  - `GET /assets/index-*.js` → 200 → Returns JS bundle from dist/assets/
  - `GET /assets/index-*.css` → 200 → Returns CSS bundle from dist/assets/

**Step 3: Verify API endpoints still work**
Test that existing API endpoints are not broken by the new catch-all route:

1. Test video feed endpoint (if camera available):
   - URL: `http://localhost:5000/video_feed?camera_id=0`
   - Expected: MJPEG stream or error message (not HTML)

2. Test image detection endpoint:
   - Use Postman/curl to POST image to `http://localhost:5000/detect/image`
   - Expected: JSON response with `{"ok": true, "count": N, "image_url": "/results/..."}`

3. Test results serving:
   - URL: `http://localhost:5000/results/images/det_xxx.jpg` (if result exists)
   - Expected: Image file returned, not HTML

**Step 4: Verify catch-all route works**
Navigate to a non-existent route: `http://localhost:5000/some/random/path`

Expected behavior:
- Returns index.html (the Vue.js app loads)
- Vue Router (if present) handles the route client-side
- No 404 error from Flask

**What to check:**
- [ ] SPA loads at root path (/)
- [ ] Static assets load correctly from /assets/
- [ ] API endpoints return JSON (not HTML)
- [ ] Non-API routes return index.html (SPA support)
- [ ] No route conflicts between API and SPA routes

Per BUILD-02: "Build output compatible with Flask static file serving"
Per D-07: "Flask 使用 `send_from_directory` 从 `frontend-vue/dist/` 服务静态文件"

Do NOT make any code changes in this task — it's verification-only.
  </action>
  <verify>
    <automated>
      # This is a manual verification task — automated check is basic file existence
      test -f frontend-vue/dist/index.html && \
      test -d frontend-vue/dist/assets && \
      grep -q "serve_spa_index" src/run/app.py && \
      echo "✅ Files ready for manual testing"
    </automated>
  </verify>
  <done>
Integration verified:
- Flask starts without errors
- SPA loads correctly at http://localhost:5000
- Static assets (JS/CSS) load from /assets/ paths
- All API endpoints (/video_feed, /detect, /results) return JSON correctly
- Non-API routes return index.html (SPA client-side routing support)
- No route conflicts between API and SPA serving
- Application is deployable as single Flask server (BUILD-02 satisfied)
  </done>
</task>

</tasks>

<verification>
After completing all tasks, perform full verification:

1. **Build verification:**
   - `cd frontend-vue && npm run build` completes without errors
   - `dist/index.html` exists with hashed asset references
   - `dist/assets/` contains minified JS and CSS bundles

2. **Flask serving verification:**
   - Start Flask: `python src/run/app.py`
   - Browse to http://localhost:5000
   - Verify SPA loads and assets are served correctly

3. **API endpoint verification:**
   - Test /video_feed endpoint (if camera available)
   - Test /detect/image with file upload
   - Test /detect/video with file upload
   - Verify all return JSON (not HTML)

4. **Integration verification:**
   - API calls from Vue.js app work correctly
   - Camera streaming displays in app
   - Image/video upload and detection function
   - Results display correctly

5. **Environment verification:**
   - Dev mode: `npm run dev` uses proxy (VITE_API_BASE_URL empty)
   - Production build: `npm run build` embeds Flask URL (VITE_API_BASE_URL=http://localhost:5000)

Manual E2E test execution from Phase 5 test cases:
- Test environment setup: Phase 5 test-cases/00-test-environment-setup.md
- Camera streaming tests: Phase 5 test-cases/01-camera-streaming.md
- Image detection tests: Phase 5 test-cases/02-image-detection.md
- Video detection tests: Phase 5 test-cases/03-video-detection.md
</verification>

<success_criteria>
- [ ] Production build generates optimized dist/ directory
- [ ] dist/index.html contains hashed asset references
- [ ] dist/assets/ contains minified JS and CSS bundles
- [ ] Flask serves index.html at root route
- [ ] Flask serves static assets from /assets/ route
- [ ] All API endpoints continue working correctly
- [ ] Non-API routes return index.html (SPA support)
- [ ] No route conflicts between API and SPA serving
- [ ] Application works as single Flask server deployment
- [ ] Requirements BUILD-01 and BUILD-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-deployment/06-02-SUMMARY.md` with:
- Build configuration verification results
- Production build output details (file sizes, hashes)
- Flask routes added for SPA serving
- Integration testing results
- Deployment instructions (how to run Flask + SPA)
- Known limitations or next steps
</output>
