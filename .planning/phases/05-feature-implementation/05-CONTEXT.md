# Phase 5: Feature Implementation - Context

**Gathered:** 2026-04-04
**Status:** Ready for planning

## Phase Boundary

Phase 5 是验证和完善阶段。Phase 4 已经实现了所有核心功能（摄像头流、图片检测、视频检测），Phase 5 专注于创建全面的测试用例，验证这些功能与 Flask 后端集成后能端到端正常工作。

**重点：** 验证 > 新功能实现。Phase 5 的产出是测试用例文档和验证清单，而不是新的代码功能。

## 实现决策

### 验证方法

- **D-01:** Phase 5 使用测试用例文档形式，每个测试用例包含：测试场景、前置条件、操作步骤、预期结果、实际结果记录区域
- **D-02:** 验证需要实际的 Flask 后端运行，不是单元测试或集成测试，而是手动端到端验证
- **D-03:** 测试结果记录在文档中，用于验证 Phase 4 实现是否满足所有需求

### 摄像头功能验证

- **D-04:** 多 ID 测试 — 验证摄像头 ID 0、1、2 等不同值都能正常启动流
- **D-05:** 启停控制 — 验证"启动"按钮开始流，"停止"按钮真正停止流（不是隐藏）
- **D-06:** 状态消息 — 验证顶部状态栏显示正确的中文消息："摄像头 N 已启动。" / "摄像头已停止。"
- **D-07:** 错误处理 — 验证当摄像头 ID 不存在或后端不可用时显示适当的错误信息

### 图片检测验证

- **D-08:** 有效文件测试 — 上传 JPG/PNG 图片，验证检测完成，显示标注结果图
- **D-09:** 无效格式测试 — 尝试上传非图片文件，验证显示错误提示
- **D-10:** 人数统计 — 验证检测结果下方正确显示检测到的人数（例如："检测到 5 人"）
- **D-11:** 空文件测试 — 未选择文件时点击检测，验证显示"请先选择一张图片"错误
- **D-12:** 按钮状态 — 验证检测期间按钮显示"检测中..."并被禁用，完成后恢复

### 视频检测验证

- **D-13:** 有效文件测试 — 上传 MP4/AVI 视频，验证处理完成后显示标注视频
- **D-14:** 无效格式测试 — 尝试上传非视频文件，验证显示错误提示
- **D-15:** 处理状态 — 验证长时间处理时按钮显示"检测中..."并被禁用
- **D-16:** 统计信息 — 验证结果下方显示视频统计（例如："总帧数：300，平均 FPS：24.5"）
- **D-17:** 视频播放 — 验证结果视频可以正常播放，有播放控制
- **D-18:** 错误处理 — 验证网络错误或超时时显示适当的错误信息

### 边界情况

- **D-19:** 网络失败 — 模拟后端关闭，验证显示友好的错误消息而非技术错误
- **D-20:** 超时处理 — 验证长时间处理时（60秒超时）有适当的反馈
- **D-21:** 大文件 — 验证大图片/视频文件能正确上传和处理
- **D-22:** 格式不支持 — 尝试上传不支持的文件格式，验证显示清晰的错误消息

### 交互状态

- **D-23:** 按钮禁用 — 验证处理期间按钮正确禁用，防止重复提交
- **D-24:** 加载反馈 — 验证用户能清楚知道系统正在处理（按钮文字变化、禁用状态）
- **D-25:** 错误恢复 — 验证错误发生后用户可以重试操作，无需刷新页面

### Claude 自由裁量

- 测试用例文档的具体格式和布局
- 测试环境的配置说明（Flask 后端启动方式）
- 实际结果记录区域的格式（文本输入、勾选框等）
- 测试通过/失败的判定标准

## 规范引用

**下游代理在规划或实现前必须阅读这些内容。**

### Phase 4 已实现功能
- `frontend-vue/src/App.vue` — 所有事件处理器已实现（handleCameraStart/handleCameraStop/handleImageDetect/handleVideoDetect）
- `frontend-vue/src/api/client.ts` — Axios 实例和辅助函数（detectImage/detectVideo）
- `frontend-vue/src/components/panels/CameraPanel.vue` — 摄像头面板组件
- `frontend-vue/src/components/panels/ImagePanel.vue` — 图片面板组件
- `frontend-vue/src/components/panels/VideoPanel.vue` — 视频面板组件

### 原始前端参考
- `frontend/index.html` 行 218-292 — 原始 JavaScript 事件处理器和错误处理逻辑

### 需求映射
- `.planning/REQUIREMENTS.md` — CAM-01 至 CAM-05、IMG-01 至 IMG-05、VID-01 至 VID-06 定义验收标准

### Phase 4 设计决策
- `.planning/phases/04-api-state-layer/04-CONTEXT.md` — D-01 至 D-18：API 集成、错误处理、状态更新模式

## 现有代码见解

### 可复用资产

- **所有事件处理器** — Phase 4 已完整实现，Phase 5 只需验证它们是否按预期工作
- **错误处理模式** — 混合方法（内联错误 + StatusMonitor）已实现
- **加载状态管理** — `processing.camera/image/video` 对象已实现并绑定到按钮禁用状态
- **中文状态消息** — 所有用户可见的消息已本地化为中文

### 已建立的模式

- **事件驱动架构** — 子组件发出事件，父组件处理 API 调用
- **响应式状态** — Vue refs 触发 UI 更新
- **时间戳缓存破坏** — 所有结果 URL 包含 `?t=${Date.now()}` 防止浏览器缓存

### 集成点

- **Flask 后端** — 验证需要运行 `python src/run/app.py`
- **Vite 开发服务器** — 验证需要运行 `cd frontend-vue && npm run dev`
- **浏览器测试** — 在浏览器中手动执行测试用例

## 具体想法

- 原始前端使用的中文消息："摄像头 N 已启动。"、"请先选择一张图片。"、"正在进行视频检测，耗时可能较长，请稍候。"
- 按钮在处理期间文字变为"检测中..."或"处理中，请稍候..."
- 视频检测完成后尝试自动播放（`video.play().catch(...)` 忽略自动播放阻止错误）
- 结果图片/视频 URL 包含时间戳参数防止浏览器缓存

## 延迟的想法

- **自动化测试** — 不创建单元测试或 E2E 测试，仅手动验证（Phase 5 范围）
- **CI/CD 集成** — 测试验证是手动的，不集成到自动化流程
- **性能测试** — 不测试大文件上传的性能或流媒体延迟
- **跨浏览器测试** — 仅验证现代浏览器（Chrome/Edge），不考虑旧版浏览器兼容性

---

*阶段: 05-feature-implementation*
*上下文收集时间: 2026-04-04*
