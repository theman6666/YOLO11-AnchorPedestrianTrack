---
phase: 02-styling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-vue/src/App.vue
  - frontend-vue/src/components/layout/HeroSection.vue
  - frontend-vue/src/components/layout/PanelGrid.vue
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
user_setup: []

must_haves:
  truths:
    - "Application displays in dark mode by default with industrial color palette"
    - "Hero section renders with title, description, and three badges"
    - "3-panel grid layout renders with responsive behavior matching original"
    - "Layout container has grid background pattern with max-width 1180px"
    - "Typography uses Inter font with consistent heading sizes and line heights"
  artifacts:
    - path: "frontend-vue/src/components/layout/HeroSection.vue"
      provides: "Hero section with title, description, and badges"
      min_lines: 40
      contains: "template with h1, p, flex-wrap for badges"
    - path: "frontend-vue/src/components/layout/PanelGrid.vue"
      provides: "3-panel grid wrapper with responsive auto-fit layout"
      min_lines: 30
      contains: "grid grid-cols-[repeat(auto-fit,minmax(340px,1fr))]"
    - path: "frontend-vue/src/App.vue"
      provides: "Root layout with page container and grid background"
      min_lines: 40
      contains: "max-w-[1180px], grid background pattern, dark mode default"
  key_links:
    - from: "frontend-vue/src/App.vue"
      to: "HeroSection.vue and PanelGrid.vue"
      via: "component imports in template"
      pattern: "<HeroSection />|<PanelGrid />"
    - from: "HeroSection.vue"
      to: "tailwind.config.js color palette"
      via: "accent-600, accent-500 classes"
      pattern: "text-accent-600 dark:text-accent-500"
    - from: "PanelGrid.vue"
      to: "original frontend grid structure"
      via: "grid-template-columns with auto-fit minmax"
      pattern: "grid-cols-\\[repeat\\(auto-fit,minmax\\(340px,1fr\\)\\)\\]"
---

<objective>
Create the foundational layout structure for the YOLO11 frontend, implementing the hero section and 3-panel grid container that match the original frontend's visual design while establishing the industrial dark mode aesthetic.

Purpose: Establish the visual foundation that all subsequent components will build upon, ensuring layout fidelity to the original while leveraging Vue 3 component architecture.
Output: Working layout with hero section and responsive 3-panel grid, dark mode enabled by default
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/index.html
@.planning/phases/02-styling-foundation/02-CONTEXT.md
@.planning/phases/02-styling-foundation/02-RESEARCH.md
@frontend-vue/tailwind.config.js
@frontend-vue/src/assets/main.css
@.planning/phases/01-project-setup/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Phase 1 and original frontend -->

From frontend/index.html (lines 36-69):
```html
<section class="hero">
  <h1>行人检测与跟踪系统</h1>
  <p>基于 YOLO11 + CBAM + ByteTrack，支持摄像头实时推理、图片检测和离线视频分析。</p>
  <div class="badge-row">
    <span class="badge">目标类别：行人</span>
    <span class="badge">跟踪算法：ByteTrack</span>
    <span class="badge">输出：人数统计 + FPS + 标注结果</span>
  </div>
</section>
```

From frontend/index.html (lines 71, 156-189):
```html
<div class="grid">
  <section class="card"><!-- Camera panel --></section>
  <section class="card"><!-- Image panel --></section>
  <section class="card"><!-- Video panel --></section>
</div>
```

Grid specification: `grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 14px;`

From frontend-vue/tailwind.config.js:
```javascript
colors: {
  gray: { 850: '#1f2937', 900: '#111827', 950: '#030712' },
  accent: { 50: '#eff6ff', 100: '#dbeafe', 500: '#3b82f6', 600: '#2563eb', 700: '#1d4ed8' }
}
```

Dark mode: `darkMode: 'class'` (toggle .dark on documentElement)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection component with title, description, and badges</name>
  <files>frontend-vue/src/components/layout/HeroSection.vue</files>
  <read_first>
    - frontend/index.html (lines 36-69 for hero structure and badge styling)
    - frontend-vue/tailwind.config.js (for accent color palette)
    - .planning/phases/02-styling-foundation/02-CONTEXT.md (decisions D-02, D-08, D-09, D-10, D-11, D-12)
  </read_first>
  <action>
Create HeroSection.vue component with exact structure from original frontend:

**Template structure:**
- Root `<section>` with classes: `bg-gradient-to-br from-white to-gray-50 dark:from-gray-800 dark:to-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg p-[22px] mb-4`
- `<h1>` with text "行人检测与跟踪系统", classes: `text-3xl font-semibold text-accent-600 dark:text-accent-500 leading-relaxed` (per D-10, D-11, D-12)
- `<p>` with description text, classes: `text-sm text-gray-600 dark:text-gray-400 leading-relaxed mt-2.5`
- Badge container `<div>` with classes: `flex flex-wrap gap-2 mt-3.5`
- Three badge `<span>` elements with classes: `inline-flex items-center px-2.5 py-1.5 text-xs font-medium rounded-full border border-blue-200 dark:border-blue-800 text-blue-700 dark:text-blue-300 bg-blue-50 dark:bg-blue-900/30`

**Badge content (exact text from original):**
1. "目标类别：行人"
2. "跟踪算法：ByteTrack"
3. "输出：人数统计 + FPS + 标注结果"

**Script section:**
- Use `<script setup lang="ts">`
- No props needed (static content)
- No reactive state needed

**Style section:**
- Use `<style scoped>` (empty, no custom CSS needed)
- All styling via Tailwind utility classes

**Color mapping per CONTEXT.md D-02:**
- Blue badges use Tailwind's blue color scale (blue-50/200/700 for light, blue-900/800/300 for dark)
- Hero title uses accent-600 (light) / accent-500 (dark) from tailwind.config.js
- Background gradient uses white-to-gray-50 (light) / gray-800-to-gray-900 (dark)
  </action>
  <verify>
    <automated>cd frontend-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend-vue/src/components/layout/HeroSection.vue exists and is >40 lines
    - File contains `<h1>行人检测与跟踪系统</h1>`
    - File contains three badge spans with exact text: "目标类别：行人", "跟踪算法：ByteTrack", "输出：人数统计 + FPS + 标注结果"
    - Template uses `text-accent-600 dark:text-accent-500` on h1 element
    - Template uses `bg-gradient-to-br from-white to-gray-50 dark:from-gray-800 dark:to-gray-900` on root section
    - All badge spans have class `rounded-full border border-blue-200 dark:border-blue-800`
    - npm run build completes without errors
  </acceptance_criteria>
  <done>
HeroSection component renders with title, description, and three badges using industrial color palette. Dark mode variants applied to all colors (backgrounds, text, borders). Component is self-contained with no external dependencies beyond Tailwind CSS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PanelGrid component with responsive 3-panel layout</name>
  <files>frontend-vue/src/components/layout/PanelGrid.vue</files>
  <read_first>
    - frontend/index.html (lines 71, 156-189 for grid structure)
    - .planning/phases/02-styling-foundation/02-CONTEXT.md (decisions D-05, D-06)
    - .planning/phases/02-styling-foundation/02-RESEARCH.md (Pattern 2: Responsive 3-Panel Grid)
  </read_first>
  <action>
Create PanelGrid.vue component that provides the responsive grid container:

**Template structure:**
- Root `<div>` with classes: `grid grid-cols-[repeat(auto-fit,minmax(340px,1fr))] gap-[14px]`
- Use `<slot />` for content injection (panels will be added in Phase 3)

**Script section:**
- Use `<script setup lang="ts">`
- No props needed (layout-only component)
- No reactive state needed

**Style section:**
- Use `<style scoped>` (empty, no custom CSS needed)

**Grid specification per CONTEXT.md D-05:**
- Exact replication: `grid-template-columns: repeat(auto-fit, minmax(340px, 1fr))`
- Tailwind arbitrary value: `grid-cols-[repeat(auto-fit,minmax(340px,1fr))]`
- Gap: 14px → Tailwind arbitrary value: `gap-[14px]`

**Rationale for arbitrary values:**
- D-05 explicitly requires "exact grid behavior from original"
- Original uses minmax(340px, 1fr) which is not in Tailwind's default scale
- 14px gap is not in Tailwind's spacing scale (closest is gap-3.5 = 14px, but D-06 says use approximations, arbitrary values acceptable for exact replication)
  </action>
  <verify>
    <automated>cd frontend-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend-vue/src/components/layout/PanelGrid.vue exists and is >20 lines
    - Template contains `<div class="grid grid-cols-[repeat(auto-fit,minmax(340px,1fr))] gap-[14px]">`
    - Template contains `<slot />` for content injection
    - File uses `<script setup lang="ts">`
    - npm run build completes without errors
  </acceptance_criteria>
  <done>
PanelGrid component provides responsive 3-panel grid container matching original frontend's exact behavior. Grid auto-fits with minimum 340px panel width, 14px gap between panels. Component accepts children via slot for flexibility in Phase 3 panel implementation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update App.vue with page container, grid background, and layout components</name>
  <files>frontend-vue/src/App.vue</files>
  <read_first>
    - frontend-vue/src/App.vue (current state from Phase 1)
    - frontend/index.html (lines 24-34 for page container and grid background)
    - .planning/phases/02-styling-foundation/02-CONTEXT.md (decisions D-01, D-04, D-06, D-09)
    - .planning/phases/01-project-setup/01-02-SUMMARY.md (dark mode toggle pattern)
  </read_first>
  <action>
Replace App.vue content with production layout structure:

**Template structure:**
1. Root `<div>` with classes: `min-h-screen bg-gray-50 dark:bg-gray-900 transition-colors`
   - Add inline style for grid background pattern: `style="background-image: linear-gradient(90deg, rgba(31,78,121,0.03) 1px, transparent 1px), linear-gradient(rgba(31,78,121,0.03) 1px, transparent 1px); background-size: 36px 36px;"`
   - This matches original frontend lines 27-30

2. Page container `<div>` with classes: `max-w-[1180px] mx-auto p-[28px_20px_36px]`
   - Matches original line 34: `padding: 28px 20px 36px`

3. Import and render HeroSection: `<HeroSection />`

4. Import and render PanelGrid with three placeholder cards:
```vue
<PanelGrid>
  <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg p-4">
    <h2 class="text-xl font-semibold text-gray-900 dark:text-white">摄像头实时检测</h2>
    <div class="text-sm text-gray-600 dark:text-gray-400 mt-1.5 mb-3.5">实时视频流分析，画面叠加跟踪 ID 与 FPS。</div>
    <!-- Content added in Phase 3 -->
  </div>
  <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg p-4">
    <h2 class="text-xl font-semibold text-gray-900 dark:text-white">单张图片检测</h2>
    <div class="text-sm text-gray-600 dark:text-gray-400 mt-1.5 mb-3.5">上传单张图片并输出检测标注结果。</div>
    <!-- Content added in Phase 3 -->
  </div>
  <div class="bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl shadow-lg p-4">
    <h2 class="text-xl font-semibold text-gray-900 dark:text-white">离线视频分析</h2>
    <div class="text-sm text-gray-600 dark:text-gray-400 mt-1.5 mb-3.5">上传视频并输出带跟踪轨迹与标注的结果视频。</div>
    <!-- Content added in Phase 3 -->
  </div>
</PanelGrid>
```

5. Add dark mode toggle button in top-right corner (floating):
```vue
<button
  @click="toggleDarkMode"
  class="fixed top-4 right-4 z-50 p-2 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow-md hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
  aria-label="Toggle dark mode"
>
  <!-- Sun icon for light mode, Moon icon for dark mode - using Lucide icons -->
  <Sun v-if="isDark" :size="20" class="text-yellow-500" />
  <Moon v-else :size="20" class="text-gray-600" />
</button>
```

**Script section:**
- Import components: `import HeroSection from '@/components/layout/HeroSection.vue'` and `import PanelGrid from '@/components/layout/PanelGrid.vue'`
- Import icons: `import { Sun, Moon } from 'lucide-vue-next'`
- Keep existing `toggleDarkMode()` function
- Add reactive state: `const isDark = ref(document.documentElement.classList.contains('dark'))`
- Add watcher to sync isDark with actual DOM state
- Initialize dark mode on mount per D-01: `onMounted(() => { document.documentElement.classList.add('dark'); isDark.value = true })`

**Style section:**
- Keep `<style scoped>` empty

**Color usage per CONTEXT.md:**
- Dark backgrounds: gray-800, gray-900 (per D-04)
- Text colors: gray-900/white for headings, gray-600/gray-400 for descriptions
- Borders: gray-200 (light), gray-700 (dark)
  </action>
  <verify>
    <automated>cd frontend-vue && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - App.vue imports HeroSection and PanelGrid components
    - Template contains `max-w-[1180px] mx-auto p-[28px_20px_36px]` on page container
    - Template contains inline style with `background-image: linear-gradient` and `background-size: 36px 36px`
    - Template renders `<HeroSection />` component
    - Template renders `<PanelGrid>` with three placeholder divs (card styling)
    - Three placeholder cards have titles: "摄像头实时检测", "单张图片检测", "离线视频分析"
    - Script contains `onMounted(() => { document.documentElement.classList.add('dark') })`
    - Dark mode toggle button present with Sun/Moon icons
    - npm run build completes without errors
  </acceptance_criteria>
  <done>
App.vue renders complete page layout with hero section, 3-panel grid, and grid background pattern. Dark mode enabled by default per D-01. Toggle button positioned in top-right corner. Placeholder cards demonstrate layout structure with proper styling (gray backgrounds, borders, shadows, rounded corners). Typography uses Inter font with consistent sizing (text-xl for headings, text-sm for descriptions).
  </done>
</task>

</tasks>

<verification>
Start dev server: `cd frontend-vue && npm run dev`
Verify in browser:
1. Page loads with dark mode active by default (background is dark gray)
2. Hero section displays with blue title, description text, and three pill-shaped badges
3. Three panel cards render in responsive grid (on desktop: 3 columns, on tablet: 2 columns, on mobile: 1 column)
4. Grid background pattern visible (subtle 36px grid)
5. Toggle button in top-right corner switches between light/dark mode
6. All text colors have proper contrast in both modes
7. Resize browser window to 1024px, 768px, 640px — panels reflow correctly
</verification>

<success_criteria>
1. Application displays in dark mode with industrial color palette (dark backgrounds, accent colors, proper contrast)
2. Hero section renders with Chinese title, description, and three badges matching original frontend
3. 3-panel grid layout renders with responsive behavior (auto-fit minmax 340px)
4. Layout container has grid background pattern with max-width 1180px and proper padding
5. Typography and spacing are consistent (Inter font, text-xl headings, text-sm body text, proper line heights)
6. Dark mode toggle functional and persists across page interactions
7. Build completes without errors and generates optimized assets
</success_criteria>

<output>
After completion, create `.planning/phases/02-styling-foundation/02-01-SUMMARY.md` with:
- Files created (HeroSection.vue, PanelGrid.vue)
- Files modified (App.vue)
- Component structure and props
- Dark mode implementation details
- Verification results
- Requirements satisfied (STYLE-01, STYLE-02, STYLE-03, STYLE-04)
</output>
