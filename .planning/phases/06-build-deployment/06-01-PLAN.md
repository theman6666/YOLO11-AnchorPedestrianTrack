---
phase: 06-build-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-vue/.env.development
  - frontend-vue/.env.production
  - frontend-vue/.gitignore
autonomous: true
requirements:
  - BUILD-03
  - BUILD-04

must_haves:
  truths:
    - "Development build uses empty VITE_API_BASE_URL (triggers Vite proxy)"
    - "Production build uses full Flask URL (e.g., http://localhost:5000)"
    - "Build command `npm run build` creates production build by default"
    - "Build command `npm run build --mode development` creates development build"
    - "Environment files are gitignored except for .env.example template"
  artifacts:
    - path: "frontend-vue/.env.development"
      provides: "Development environment configuration"
      contains: "VITE_API_BASE_URL="
    - path: "frontend-vue/.env.production"
      provides: "Production environment configuration"
      contains: "VITE_API_BASE_URL=http://localhost:5000"
    - path: "frontend-vue/.gitignore"
      provides: "Git ignore rules for environment files"
      contains: ".env.development\n.env.production"
  key_links:
    - from: "vite build command"
      to: ".env.development or .env.production"
      via: "Vite mode selection (--mode flag)"
      pattern: "vite build --mode (development|production)"
    - from: "src/api/client.ts"
      to: "import.meta.env.VITE_API_BASE_URL"
      via: "Build-time environment variable injection"
      pattern: "baseURL: import.meta.env.VITE_API_BASE_URL"
---

<objective>
Configure environment variable system for development and production builds with proper API base URL settings.

Purpose: Enable the same codebase to work in both development (with Vite proxy) and production (direct Flask connection) modes through environment-based configuration.
Output: Environment files (.env.development, .env.production) with proper API URL configuration and gitignore rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-build-deployment/06-CONTEXT.md
@.planning/phases/06-build-deployment/06-RESEARCH.md
@.planning/REQUIREMENTS.md

# Reference files for implementation patterns
@frontend-vue/.env.example
@frontend-vue/vite.config.ts
@frontend-vue/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create development environment file</name>
  <files>frontend-vue/.env.development</files>
  <read_first>
    - frontend-vue/.env.example
    - frontend-vue/src/api/client.ts
    - frontend-vue/vite.config.ts
  </read_first>
  <action>
Create `.env.development` file in `frontend-vue/` directory with the following content:

```bash
# Development Environment Configuration
# This file is loaded when running: vite or vite build --mode development

# API Base URL
# Leave empty to use Vite proxy configured in vite.config.ts
# The proxy forwards /video_feed, /detect, and /results to Flask backend at localhost:5000
VITE_API_BASE_URL=
```

This empty string configuration ensures that during development:
1. Axios baseURL becomes empty string (per D-05)
2. API requests use relative URLs like `/detect/image`
3. Vite dev server proxies these requests to Flask backend at localhost:5000
4. No CORS issues since proxy runs on same origin

Reference D-04 and D-05 from CONTEXT.md for environment variable requirements.
Reference vite.config.ts proxy configuration at lines 28-50.
  </action>
  <verify>
    <automated>test -f frontend-vue/.env.development && grep -q "^VITE_API_BASE_URL=$" frontend-vue/.env.development && echo "✅ .env.development created with empty API base URL"</automated>
  </verify>
  <done>
File exists at frontend-vue/.env.development with:
- VITE_API_BASE_URL set to empty string (triggers Vite proxy per D-05)
- Comment explaining proxy behavior
- Ready for Vite to load automatically in development mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Create production environment file</name>
  <files>frontend-vue/.env.production</files>
  <read_first>
    - frontend-vue/.env.example
    - frontend-vue/src/api/client.ts
    - frontend-vue/vite.config.ts
  </read_first>
  <action>
Create `.env.production` file in `frontend-vue/` directory with the following content:

```bash
# Production Environment Configuration
# This file is loaded when running: vite build (default mode)

# API Base URL
# Set to the full URL where Flask backend is running
# Change this to your actual deployment URL (e.g., http://your-server.com:5000)
VITE_API_BASE_URL=http://localhost:5000
```

This configuration ensures that in production:
1. Axios baseURL is set to `http://localhost:5000` (per D-05)
2. API requests use absolute URLs like `http://localhost:5000/detect/image`
3. No proxy is involved — browser connects directly to Flask backend
4. Works correctly when Flask serves the SPA from dist/ directory

Reference D-04 and D-05 from CONTEXT.md for environment variable requirements.
Reference src/api/client.ts line 7 showing baseURL usage: `baseURL: import.meta.env.VITE_API_BASE_URL || ''`
  </action>
  <verify>
    <automated>test -f frontend-vue/.env.production && grep -q "^VITE_API_BASE_URL=http://localhost:5000" frontend-vue/.env.production && echo "✅ .env.production created with Flask URL"</automated>
  </verify>
  <done>
File exists at frontend-vue/.env.production with:
- VITE_API_BASE_URL set to http://localhost:5000 (per D-05)
- Comment explaining deployment URL configuration
- Ready for Vite to load automatically in production build mode
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore for environment files</name>
  <files>frontend-vue/.gitignore</files>
  <read_first>
    - frontend-vue/.gitignore
  </read_first>
  <action>
Update `.gitignore` file in `frontend-vue/` directory to add environment file exclusions.

First, read the existing .gitignore to understand current patterns. Then add the following lines if not already present:

```
# Environment files (deployment-specific configuration)
.env.development
.env.production
.env
```

Add these lines in an appropriate section (e.g., after other ignored files, before build artifacts). If a "# Environment variables" or similar section exists, add the lines there.

This ensures:
1. Deployment-specific URLs are not committed to git (per D-06)
2. .env.example remains as the template (already in repository)
3. Each developer/deployment can have their own local environment configuration
4. No accidental commits of production URLs

Reference D-06 from CONTEXT.md: ".env.example 文件作为模板存在，不包含敏感信息"
  </action>
  <verify>
    <automated>grep -q "^\.env.development" frontend-vue/.gitignore && grep -q "^\.env.production" frontend-vue/.gitignore && echo "✅ .gitignore updated with environment files"</automated>
  </verify>
  <done>
File frontend-vue/.gitignore contains:
- .env.development exclusion (local dev config not committed)
- .env.production exclusion (deployment-specific URLs not committed)
- .env exclusion (catch-all for local overrides)
- .env.example remains tracked (template for developers)
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify environment switching behavior</name>
  <files>
    - frontend-vue/.env.development
    - frontend-vue/.env.production
    - frontend-vue/src/api/client.ts
  </files>
  <read_first>
    - frontend-vue/.env.development
    - frontend-vue/.env.production
    - frontend-vue/src/api/client.ts
    - frontend-vue/package.json
  </read_first>
  <action>
Verify that the environment variable system works correctly by checking the build configuration.

This task validates that:
1. Vite loads the correct .env file based on mode (development vs production)
2. The API client correctly uses `import.meta.env.VITE_API_BASE_URL`
3. Build scripts in package.json support both modes

From package.json, verify these scripts exist:
- `"build": "run-p type-check \"build-only {@}\" --"` — runs production build by default
- `"build-only": "vite build"` — invokes Vite build (defaults to production mode)
- `"dev": "vite"` — runs dev server (automatically loads .env.development)

From src/api/client.ts, verify line 7:
- `baseURL: import.meta.env.VITE_API_BASE_URL || ''`
- This correctly reads the environment variable at build time
- Falls back to empty string if not set (safe default for dev proxy)

Do NOT make any changes — this is a verification task to confirm the setup is correct.

Per D-11: "Vite 根据 `NODE_ENV` 或 `--mode` 自动选择正确的 .env 文件"
Per D-12: "不创建单独的 build:dev/build:prod 脚本" — we rely on Vite's automatic selection
  </action>
  <verify>
    <automated>
      # Verify .env files exist
      test -f frontend-vue/.env.development && test -f frontend-vue/.env.production && \
      # Verify package.json has build scripts
      grep -q '"build":' frontend-vue/package.json && \
      grep -q '"dev":' frontend-vue/package.json && \
      # Verify API client uses environment variable
      grep -q "import.meta.env.VITE_API_BASE_URL" frontend-vue/src/api/client.ts && \
      echo "✅ Environment switching verified"
    </automated>
  </verify>
  <done>
Environment configuration verified:
- .env.development exists with empty VITE_API_BASE_URL (proxy mode)
- .env.production exists with http://localhost:5000 (direct connection)
- package.json has build and dev scripts that use Vite's automatic mode selection
- src/api/client.ts correctly imports and uses VITE_API_BASE_URL
- System supports both development and production builds per BUILD-03 and BUILD-04
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify the environment variable system:

1. Check that .env.development and .env.production exist in frontend-vue/
2. Verify .gitignore excludes both environment files
3. Confirm package.json scripts don't need changes (Vite handles mode selection)
4. Test that dev server can be started with `npm run dev` (uses .env.development)
5. Test that production build can be created with `npm run build` (uses .env.production)

Manual verification (after Phase 6-02 builds are complete):
- Build production: `cd frontend-vue && npm run build`
- Check embedded API URL: `grep -o 'baseURL:"[^"]*"' dist/assets/index-*.js | head -1`
- Should show: `baseURL:"http://localhost:5000"`
</verification>

<success_criteria>
- [ ] .env.development file created with empty VITE_API_BASE_URL
- [ ] .env.production file created with Flask URL
- [ ] .gitignore updated to exclude environment files
- [ ] Environment switching works correctly (dev vs production builds)
- [ ] Build scripts support both modes without custom scripts
- [ ] Requirements BUILD-03 and BUILD-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-deployment/06-01-SUMMARY.md` with:
- Files created (.env.development, .env.production)
- Files modified (.gitignore)
- Environment configuration details
- Verification results
- How to use: dev mode vs production build
</output>
