---
phase: 03-component-architecture
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - frontend-vue/src/App.vue
autonomous: true
requirements:
  - COMP-01
user_setup: []
must_haves:
  truths:
    - "App.vue renders all three panel components (CameraPanel, ImagePanel, VideoPanel)"
    - "All panels render within PanelGrid in correct positions"
    - "CameraPanel is in first grid position"
    - "ImagePanel is in second grid position"
    - "VideoPanel is in third grid position"
    - "StatusMonitor displays below PanelGrid with reactive state"
    - "All panel event handlers are wired to parent methods"
    - "Placeholder content divs are removed and replaced with panel components"
  artifacts:
    - path: "frontend-vue/src/App.vue"
      provides: "Root application component with panel integration"
      min_lines: 100
      exports: ["default"]
  key_links:
    - from: "App.vue panel event handlers"
      to: "Phase 4 API client integration"
      via: "Console logging placeholders for future implementation"
      pattern: "console\\.log\\('Start camera:', cameraId\\)"
    - from: "Panel components"
      to: "Parent state management"
      via: "Reactive refs for processing flags and status messages"
      pattern: ":processing=\"processing\\.camera\""
---

<objective>
Update App.vue to integrate all three panel components (CameraPanel, ImagePanel, VideoPanel) with proper event handling and state management, completing the component architecture phase.

Purpose: Replace placeholder content divs with fully functional panel components, establish the emit-based event handling pattern that Phase 4 will connect to API calls, and provide reactive state for processing flags and status messages.

Output: Updated App.vue with all panel components integrated, event handlers wired, and reactive state management for Phase 4 API integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-component-architecture/03-CONTEXT.md
@.planning/phases/03-component-architecture/03-RESEARCH.md
@.planning/REQUIREMENTS.md

@frontend-vue/src/App.vue (current App.vue with placeholder content)
@frontend-vue/src/components/panels/CameraPanel.vue (camera panel component)
@frontend-vue/src/components/panels/ImagePanel.vue (image panel component)
@frontend-vue/src/components/panels/VideoPanel.vue (video panel component)
@.planning/phases/02-styling-foundation/02-02-SUMMARY.md (App.vue pattern reference)

# User Decisions from CONTEXT.md
- D-13: State lifted to App.vue parent (statusMessage, statusIsOk, processing flags)
- D-14: Components emit events; parent handles API calls and updates state (Phase 4)

# Current App.vue Structure
From existing App.vue:
- Has statusMessage and statusIsOk refs (from Phase 2)
- Has HeroSection and PanelGrid components (from Phase 2)
- Has three placeholder Card components with "[...面板内容待实现]" text
- StatusMonitor already wired with reactive state

# Integration Requirements from RESEARCH.md
Section: Code Examples → App.vue Integration
- Processing state object with separate flags for camera, image, video
- Event handlers for @start, @stop (CameraPanel) and @detect (ImagePanel, VideoPanel)
- Console.log placeholders for Phase 4 API implementation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.vue to integrate all panel components with event handling</name>
  <files>frontend-vue/src/App.vue</files>
  <read_first>
    - frontend-vue/src/App.vue (current state with placeholder content)
    - frontend-vue/src/components/panels/CameraPanel.vue (camera panel API)
    - frontend-vue/src/components/panels/ImagePanel.vue (image panel API)
    - frontend-vue/src/components/panels/VideoPanel.vue (video panel API)
    - .planning/phases/03-component-architecture/03-RESEARCH.md (App.vue Integration example)
  </read_first>
  <action>
Update App.vue to integrate all panel components with the following changes:

**Script Section Updates:**
Add imports for panel components and processing state:

```typescript
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { Sun, Moon } from 'lucide-vue-next'
import HeroSection from '@/components/layout/HeroSection.vue'
import PanelGrid from '@/components/layout/PanelGrid.vue'
import StatusMonitor from '@/components/layout/StatusMonitor.vue'
import CameraPanel from '@/components/panels/CameraPanel.vue'
import ImagePanel from '@/components/panels/ImagePanel.vue'
import VideoPanel from '@/components/panels/VideoPanel.vue'

// Reactive state for dark mode
const isDark = ref(false)

// Reactive state for status message
const statusMessage = ref('系统就绪。')
const statusIsOk = ref(false)

// Reactive state for processing flags (per D-13)
const processing = ref({
  camera: false,
  image: false,
  video: false,
})

// Reactive state for panel results (for Phase 4 integration)
const cameraStreamUrl = ref<string | undefined>(undefined)
const imageResultUrl = ref<string | undefined>(undefined)
const imagePersonCount = ref<number | undefined>(undefined)
const imageErrorMessage = ref<string | undefined>(undefined)
const videoResultUrl = ref<string | undefined>(undefined)
const videoStats = ref<{ frames: number; avg_fps: number } | undefined>(undefined)
const videoErrorMessage = ref<string | undefined>(undefined)

// Toggle dark mode
const toggleDarkMode = () => {
  document.documentElement.classList.toggle('dark')
  isDark.value = document.documentElement.classList.contains('dark')
}

// Initialize dark mode on mount (default to dark per D-01)
onMounted(() => {
  document.documentElement.classList.add('dark')
  isDark.value = true
})

// Camera panel event handlers (Phase 4 will implement API calls)
const handleCameraStart = (cameraId: number) => {
  console.log('Start camera:', cameraId)
  statusMessage.value = `摄像头 ${cameraId} 已启动。`
  statusIsOk.value = true
  // Phase 4: Set cameraStreamUrl.value = `/video_feed?camera_id=${cameraId}`
}

const handleCameraStop = () => {
  console.log('Stop camera')
  statusMessage.value = '摄像头已停止。'
  statusIsOk.value = false
  cameraStreamUrl.value = undefined
  // Phase 4: Clear stream by setting src to empty string
}

// Image panel event handler (Phase 4 will implement API call)
const handleImageDetect = async (file: File) => {
  console.log('Detect image:', file.name)
  statusMessage.value = '正在进行图片检测...'
  statusIsOk.value = false
  processing.value.image = true
  imageErrorMessage.value = undefined

  try {
    // Phase 4: Implement API call to /detect/image
    // const formData = new FormData()
    // formData.append('file', file)
    // const response = await fetch('/detect/image', { method: 'POST', body: formData })
    // const data = await response.json()

    // Simulate API call for now
    await new Promise(resolve => setTimeout(resolve, 1000))

    // Phase 4: Update with real data
    // imageResultUrl.value = data.image_url
    // imagePersonCount.value = data.count
    // statusMessage.value = data.message || '图片检测完成。'
    // statusIsOk.value = true

    statusMessage.value = '图片检测功能将在 Phase 4 实现。'
    statusIsOk.value = true
  } catch (error) {
    statusMessage.value = '图片检测出错。'
    statusIsOk.value = false
    imageErrorMessage.value = '图片检测出错。'
  } finally {
    processing.value.image = false
  }
}

// Video panel event handler (Phase 4 will implement API call)
const handleVideoDetect = async (file: File) => {
  console.log('Detect video:', file.name)
  statusMessage.value = '正在进行视频检测，耗时可能较长，请稍候。'
  statusIsOk.value = false
  processing.value.video = true
  videoErrorMessage.value = undefined

  try {
    // Phase 4: Implement API call to /detect/video
    // const formData = new FormData()
    // formData.append('file', file)
    // const response = await fetch('/detect/video', { method: 'POST', body: formData })
    // const data = await response.json()

    // Simulate API call for now
    await new Promise(resolve => setTimeout(resolve, 2000))

    // Phase 4: Update with real data
    // videoResultUrl.value = data.video_url
    // videoStats.value = data.stats
    // statusMessage.value = data.message || '视频检测完成。'
    // statusIsOk.value = true

    statusMessage.value = '视频检测功能将在 Phase 4 实现。'
    statusIsOk.value = true
  } catch (error) {
    statusMessage.value = '视频检测出错。'
    statusIsOk.value = false
    videoErrorMessage.value = '视频检测出错。'
  } finally {
    processing.value.video = false
  }
}
</script>
```

**Template Section Updates:**
Replace placeholder Card components with panel components:

```vue
<template>
  <div
    class="min-h-screen bg-gray-50 dark:bg-gray-900 transition-colors"
    style="background-image: linear-gradient(90deg, rgba(31,78,121,0.03) 1px, transparent 1px), linear-gradient(rgba(31,78,121,0.03) 1px, transparent 1px); background-size: 36px 36px;"
  >
    <div class="max-w-[1180px] mx-auto p-[28px_20px_36px]">
      <!-- Dark mode toggle button (fixed top-right) -->
      <button
        @click="toggleDarkMode"
        class="fixed top-4 right-4 z-50 p-2 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg shadow-md hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
        aria-label="Toggle dark mode"
      >
        <Sun v-if="isDark" :size="20" class="text-yellow-500" />
        <Moon v-else :size="20" class="text-gray-600" />
      </button>

      <!-- Hero Section -->
      <HeroSection />

      <!-- 3-Panel Grid -->
      <PanelGrid>
        <!-- Camera Panel -->
        <CameraPanel
          :processing="processing.camera"
          :stream-url="cameraStreamUrl"
          @start="handleCameraStart"
          @stop="handleCameraStop"
        />

        <!-- Image Panel -->
        <ImagePanel
          :processing="processing.image"
          :result-url="imageResultUrl"
          :person-count="imagePersonCount"
          :error-message="imageErrorMessage"
          @detect="handleImageDetect"
        />

        <!-- Video Panel -->
        <VideoPanel
          :processing="processing.video"
          :result-url="videoResultUrl"
          :video-stats="videoStats"
          :error-message="videoErrorMessage"
          @detect="handleVideoDetect"
        />
      </PanelGrid>

      <!-- Status Monitor -->
      <StatusMonitor :message="statusMessage" :isOk="statusIsOk" />
    </div>
  </div>
</template>

<style scoped>
/* All styling via Tailwind utility classes */
</style>
```

**Key Implementation Details:**
- Import all three panel components (CameraPanel, ImagePanel, VideoPanel)
- Create processing object with separate flags for each panel (per D-13)
- Create result state refs for each panel (cameraStreamUrl, imageResultUrl, videoResultUrl, etc.)
- Create event handler methods for all panel events (@start, @stop, @detect)
- Event handlers currently use console.log placeholders (Phase 4 will implement API calls)
- Event handlers update statusMessage and statusIsOk refs for StatusMonitor
- Replace three placeholder Card components with panel components
- Bind processing flags to each panel via :processing prop
- Bind result URLs and stats to each panel via props
- Wire event handlers via @start, @stop, @detect event bindings
- CameraPanel uses @start and @stop events
- ImagePanel uses @detect event
- VideoPanel uses @detect event
- All panels maintain their position in PanelGrid (Camera first, Image second, Video third)
- StatusMonitor remains below PanelGrid with existing reactive state
- Dark mode toggle and HeroSection remain unchanged

**Phase 4 Integration Points:**
- Event handlers have TODO comments for Phase 4 API implementation
- Result state refs are ready for Phase 4 API response data
- Processing flags are ready for Phase 4 loading state management
- Console.log statements provide debugging for event flow

**State Management Pattern:**
- All state lifted to App.vue parent (per D-13)
- Panel components are stateless — they only emit events
- Parent handles all state updates and API calls (per D-14)
- This pattern enables Phase 4 to add API client without modifying panels
  </action>
  <verify>
    <automated>cd frontend-vue && npm run build</automated>
  </verify>
  <done>
App.vue updated with:
- All three panel components imported (CameraPanel, ImagePanel, VideoPanel)
- Processing state object with camera, image, video flags
- Result state refs for all panel outputs (stream URLs, person count, video stats)
- Event handler methods: handleCameraStart, handleCameraStop, handleImageDetect, handleVideoDetect
- Event handlers use console.log placeholders for Phase 4
- Three placeholder Card components replaced with panel components
- PanelGrid contains CameraPanel, ImagePanel, VideoPanel in correct positions
- All panels have @event bindings to parent handlers
- All panels have :processing and result prop bindings
- StatusMonitor displays below PanelGrid with reactive state
- Dark mode toggle and HeroSection unchanged
- Build succeeds without errors
  </done>
</task>

</tasks>

<verification>
1. App.vue imports CameraPanel, ImagePanel, VideoPanel components
2. Script contains processing ref object with camera, image, video flags
3. Script contains result state refs (cameraStreamUrl, imageResultUrl, imagePersonCount, videoResultUrl, videoStats)
4. Script contains event handler methods: handleCameraStart, handleCameraStop, handleImageDetect, handleVideoDetect
5. Event handlers contain console.log statements for debugging
6. Event handlers update statusMessage and statusIsOk refs
7. Template contains PanelGrid with three panel components
8. PanelGrid no longer contains placeholder Card components
9. CameraPanel is first component in PanelGrid
10. ImagePanel is second component in PanelGrid
11. VideoPanel is third component in PanelGrid
12. CameraPanel has @start and @stop event bindings
13. ImagePanel has @detect event binding
14. VideoPanel has @detect event binding
15. All panels have :processing prop bindings
16. Panels have result prop bindings (:stream-url, :result-url, :person-count, :video-stats)
17. StatusMonitor remains below PanelGrid
18. HeroSection and dark mode toggle unchanged
19. Build completes successfully (verify with npm run build)
</verification>

<success_criteria>
1. Application renders with all three panel components visible
2. Camera panel displays in first grid position with input and buttons
3. Image panel displays in second grid position with file input
4. Video panel displays in third grid position with file input
5. All panels have correct Chinese titles and descriptions
6. Clicking camera start button updates status message
7. Clicking camera stop button updates status message
8. Selecting image file and clicking detect button updates status message
9. Selecting video file and clicking detect button updates status message
10. Buttons show loading state during processing (simulated with setTimeout)
11. Status monitor displays status messages with correct color coding
12. Console logs appear in browser console for all panel events
13. Dark mode toggle still works correctly
14. Responsive grid layout works on different screen sizes
15. TypeScript types compile without errors
16. Build output is error-free
17. Page loads without console errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-architecture/03-05-SUMMARY.md`
</output>
