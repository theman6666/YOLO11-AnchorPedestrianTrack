---
phase: 06-build-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/run/app.py
autonomous: false
requirements:
  - BUILD-02
gap_closure: true

must_haves:
  truths:
    - "Flask serves index.html from frontend-vue/dist/ at root route (NOT old template)"
    - "All non-API routes return index.html (SPA support)"
    - "Only ONE @app.route('/') handler exists (the SPA serving route)"
    - "API routes (/video_feed, /detect, /results) continue working after duplicate removal"
  artifacts:
    - path: "src/run/app.py"
      provides: "Flask routes for serving SPA and API"
      contains: "Exactly one @app.route('/') handler (serve_spa_index)"
      missing:
        - "Old @app.route('/') handler at lines 106-108 that returns render_template('index.html')"
  key_links:
    - from: "Browser request to /"
      to: "dist/index.html"
      via: "Flask serve_spa_index() route (NOT old index() route)"
      pattern: "@app.route\(\"\/\"\) should call send_from_directory"
    - from: "Browser request to /<path:path>"
      to: "dist/index.html"
      via: "Flask catch_all() route"
      pattern: "catch_all returns send_from_directory for non-API routes"
---

<objective>
Fix duplicate root route blocker preventing Flask from serving the Vue.js SPA, enabling BUILD-02 requirement completion.

Purpose: Remove shadowing old template route so Flask serves the production-built Vue.js application instead of legacy HTML template.
Output: Single root route serving dist/index.html, with catch-all route functioning correctly for SPA client-side routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-build-deployment/06-CONTEXT.md
@.planning/phases/06-build-deployment/06-RESEARCH.md
@.planning/phases/06-build-deployment/06-VERIFICATION.md
@.planning/phases/06-build-deployment/06-01-SUMMARY.md
@.planning/phases/06-build-deployment/06-02-SUMMARY.md
@.planning/REQUIREMENTS.md

# Reference files for implementation patterns
@src/run/app.py
@frontend-vue/dist/index.html
</context>

<interfaces>
<!-- Key context from existing code that executor must understand -->

From 06-02-SUMMARY.md:
- Plan 06-02 added SPA serving routes to app.py (commit: 86c159f)
- Routes added: serve_spa_index() at line 197, serve_assets() at line 204, serve_favicon() at line 211, catch_all() at line 218
- Existing API routes preserved at lines 108-189: /video_feed, /detect/image, /detect/video, /results/<path>
- DIST_DIR constant points to frontend-vue/dist/ (line 28)

From 06-VERIFICATION.md:
- OLD DUPLICATE ROUTE exists at lines 106-108:
  ```python
  @app.route("/")
  def index():
      return render_template("index.html")
  ```
- NEW SPA ROUTE exists at lines 197-200:
  ```python
  @app.route("/")
  def serve_spa_index():
      return send_from_directory(str(DIST_DIR), "index.html")
  ```
- Flask matches routes in definition order — FIRST matching route wins
- Old route at line 106 shadows new route at line 197 — SPA never served
- Catch-all route at line 218 cannot work because root route conflict prevents SPA from being served

Gap impact:
- Users accessing http://localhost:5000 see old HTML template instead of Vue.js SPA
- BUILD-02 requirement ("Build output compatible with Flask static file serving") is NOT satisfied
- Phase 6 goal ("Production-optimized build configured to serve alongside Flask backend") is BLOCKED
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate root route blocking SPA serving</name>
  <files>src/run/app.py</files>
  <read_first>
    - src/run/app.py
    - .planning/phases/06-build-deployment/06-VERIFICATION.md
    - .planning/phases/06-build-deployment/06-02-SUMMARY.md
  </read_first>
  <action>
Remove the old duplicate root route that is preventing Flask from serving the Vue.js SPA.

**Step 1: Locate the old root route**
In `src/run/app.py`, find the old route at lines 106-108:
```python
@app.route("/")
def index():
    return render_template("index.html")
```

**Step 2: Comment out or remove the old route**
Replace the old route (lines 106-108) with a comment explaining why it was removed:

```python
# OLD ROOT ROUTE REMOVED (2026-04-04)
# The following route was commented out to fix duplicate route handler issue.
# Flask matches routes in definition order — this old route at line 106 was
# shadowing the new SPA serving route added in Phase 06-02 at line 197.
#
# @app.route("/")
# def index():
#     return render_template("index.html")
#
# SPA is now served by serve_spa_index() function at line 197 below,
# which returns send_from_directory(str(DIST_DIR), "index.html")
# to serve the Vue.js SPA from frontend-vue/dist/index.html
```

**Step 3: Verify only one root route exists**
After commenting out the old route, search app.py for `@app.route("/")`:
- There should be EXACTLY ONE match — the `serve_spa_index()` function
- This route should use `send_from_directory(str(DIST_DIR), "index.html")`
- No route should use `render_template("index.html")`

**Critical implementation details:**
1. Do NOT modify the new SPA route at line 197 (`serve_spa_index()`) — it's correct
2. Do NOT modify API routes (/video_feed, /detect, /results) — they must be preserved
3. Do NOT modify the catch-all route at line 218 — it's correct
4. Comment style should explain WHY the old route was removed (future maintenance)
5. Keep the comment concise but informative — reference the Phase 06-02 work

Reference 06-VERIFICATION.md gap analysis:
- "Duplicate root route at line 106 (`render_template("index.html")`) shadows new SPA route at line 197"
- "Impact: Users accessing http://localhost:5000 see old HTML template instead of Vue.js SPA"
- "Required fix: Remove or comment out old root route at lines 106-108"

Reference RESEARCH.md "Pitfall 2: API Route vs SPA Route Priority":
- "Flask routes are matched in definition order"
- "If catch-all route is defined before API routes, it intercepts all requests"
- Solution: Always define API routes first, SPA routes after
  </action>
  <verify>
    <automated>
      # Verify old route is commented out (search for exact pattern from VERIFICATION.md)
      ! grep -q '^def index():' src/run/app.py && \
      # Verify only ONE root route exists (serve_spa_index)
      test $(grep -c '@app.route("/")' src/run/app.py) -eq 1 && \
      # Verify the remaining root route uses send_from_directory
      grep -A1 '@app.route("/")' src/run/app.py | grep -q 'send_from_directory.*DIST_DIR.*index.html' && \
      # Verify old render_template('index.html') is NOT in any route handler
      ! grep -q "render_template('index.html')" src/run/app.py && \
      echo "✅ Duplicate root route removed, SPA route now unique"
    </automated>
  </verify>
  <done>
  Duplicate root route blocker fixed:
  - Old `@app.route("/")` at lines 106-108 removed or commented out
  - Only ONE `@app.route("/")` handler exists: `serve_spa_index()` at line 197
  - `serve_spa_index()` returns `send_from_directory(str(DIST_DIR), "index.html")`
  - Old `render_template("index.html")` pattern no longer present in app.py
  - Flask will now serve Vue.js SPA from dist/ at root path
  - Truth "Flask serves index.html from frontend-vue/dist/ at root route" now achievable
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify catch-all route can now function correctly</name>
  <files>
    - src/run/app.py
    - frontend-vue/dist/index.html
  </files>
  <read_first>
    - src/run/app.py
    - .planning/phases/06-build-deployment/06-VERIFICATION.md
    - .planning/phases/06-build-deployment/06-02-SUMMARY.md
  </read_first>
  <action>
Verify that the catch-all route can now function correctly after fixing the duplicate root route.

**Step 1: Locate the catch-all route**
In `src/run/app.py`, find the catch-all route (should be around line 218):
```python
@app.route("/<path:path>")
def catch_all(path):
    """
    Catch-all route for SPA support.
    Returns index.html for any non-API route to support client-side routing.
    API routes (/video_feed, /detect, /results) are defined above and take priority.
    """
    # Check if the request is for an API route that should have been handled earlier
    if path.startswith(('video_feed', 'detect', 'results')):
        return f"API route /{path} not found", 404

    # Return index.html for all other routes (SPA client-side routing)
    return send_from_directory(str(DIST_DIR), "index.html")
```

**Step 2: Verify route ordering is correct**
Confirm the route definition order in app.py:
1. API routes are defined FIRST (lines ~108-189):
   - `/video_feed`
   - `/detect/image`
   - `/detect/video`
   - `/results/<path:filename>`
2. SPA serving routes are defined AFTER API routes:
   - `/` (serve_spa_index) — should now be the ONLY root route
   - `/assets/<path:filename>` (serve_assets)
   - `/favicon.ico` (serve_favicon)
3. Catch-all route is defined LAST:
   - `/<path:path>` (catch_all)

**Step 3: Verify catch-all route implementation**
The catch-all route should:
- Check if path starts with 'video_feed', 'detect', or 'results' → return 404 (API route not found)
- Return `send_from_directory(str(DIST_DIR), "index.html")` for all other paths
- Support client-side routing (Vue Router if present)

**Do NOT make any changes** unless the catch-all route is missing or broken. This is a verification task to confirm the route can function after Gap 1 is fixed.

Per 06-VERIFICATION.md secondary gap:
- "Catch-all route exists at line 218 but cannot be verified as working until root route conflict is resolved"
- "After fixing duplicate root route, test catch-all route with non-API paths"

Per D-09: "所有非 API 请求返回 `index.html`（支持 SPA 路由）"
  </action>
  <verify>
    <automated>
      # Verify catch-all route exists
      grep -q 'def catch_all' src/run/app.py && \
      # Verify catch-all route uses send_from_directory for SPA
      grep -A10 'def catch_all' src/run/app.py | grep -q 'send_from_directory.*DIST_DIR.*index.html' && \
      # Verify catch-all route checks for API paths
      grep -A10 'def catch_all' src/run/app.py | grep -q "path.startswith.*'video_feed'" && \
      # Verify catch-all route is defined AFTER root route (check line numbers)
      [ $(grep -n 'def serve_spa_index' src/run/app.py | cut -d: -f1) -lt $(grep -n 'def catch_all' src/run/app.py | cut -d: -f1) ] && \
      echo "✅ Catch-all route verified and correctly positioned"
    </automated>
  </verify>
  <done>
  Catch-all route verified as functional:
  - catch_all() function exists in app.py
  - Route definition uses `@app.route("/<path:path>")` pattern
  - Returns `send_from_directory(str(DIST_DIR), "index.html")` for non-API paths
  - Checks for API path prefixes ('video_feed', 'detect', 'results') and returns 404
  - Defined AFTER all API routes and SPA serving routes (correct ordering)
  - Truth "All non-API routes return index.html (SPA support)" now achievable
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of Flask + SPA integration</name>
  <files>
    - src/run/app.py
    - frontend-vue/dist/
  </files>
  <read_first>
    - src/run/app.py
    - frontend-vue/dist/index.html
  </read_first>
  <what-built>
    Flask app.py with duplicate root route removed — Flask now serves Vue.js SPA instead of old HTML template
  </what-built>
  <how-to-verify>
    **Step 1: Start Flask server**
    ```bash
    cd D:/YOLO11-AnchorPedestrianTrack
    python src/run/app.py
    ```
    Expected output: "Web service started: http://127.0.0.1:5000"

    **Step 2: Verify SPA serves at root path**
    Open browser to: http://localhost:5000
    Expected:
    - Vue.js application loads (dark mode UI, 3-panel layout)
    - Browser DevTools Network tab shows:
      - `GET /` → 200 → Returns dist/index.html (NOT old template)
      - `GET /assets/index-hxKpNlEh.js` → 200 → Returns JS bundle
      - `GET /assets/index-CW_ZtgX1.css` → 200 → Returns CSS bundle

    **Step 3: Verify catch-all route works**
    Navigate to: http://localhost:5000/some/random/path
    Expected:
    - Returns index.html (Vue.js app loads)
    - No 404 error from Flask
    - Vue Router handles route client-side (or shows 404 in UI if route not defined)

    **Step 4: Verify API endpoints still work**
    Test API endpoints (use browser DevTools or Postman):
    - `GET /video_feed?camera_id=0` → MJPEG stream or error (NOT HTML)
    - `POST /detect/image` with file upload → JSON response (NOT HTML)
    - `POST /detect/video` with file upload → JSON response (NOT HTML)
    - `GET /results/images/det_xxx.jpg` → Image file (NOT HTML)

    **Step 5: Verify no route conflicts**
    - SPA loads correctly at root path
    - API endpoints return JSON (not HTML)
    - Non-API paths return index.html (SPA routing)
    - No 404 errors from Flask for SPA routes

    **What to check:**
    - [ ] Vue.js SPA loads at http://localhost:5000 (not old HTML template)
    - [ ] Static assets load from /assets/ paths
    - [ ] API endpoints return JSON responses
    - [ ] Non-API routes return index.html (SPA client-side routing)
    - [ ] No duplicate root route errors in Flask logs
  </how-to-verify>
  <verify>
    <automated>MISSING — Manual verification checkpoint requires human testing of Flask server and browser</automated>
  </verify>
  <done>
  Manual verification complete:
  - Flask server starts without errors
  - Vue.js SPA loads at http://localhost:5000 (not old template)
  - Static assets (JS/CSS) load from /assets/ paths
  - API endpoints return JSON responses (not HTML)
  - Non-API routes return index.html (catch-all route works)
  - No route conflicts between API and SPA serving
  - BUILD-02 requirement satisfied: Flask serves SPA from dist/ directory
  - Gap 1 (duplicate root route): Closed
  - Gap 2 (catch-all blocked): Closed
  </done>
  <resume-signal>Type "approved" if SPA loads correctly and all API endpoints work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After completing all tasks, perform comprehensive verification:

1. **Code verification:**
   - `grep -c '@app.route("/")' src/run/app.py` returns 1 (only one root route)
   - `grep -A1 '@app.route("/")' src/run/app.py` contains `send_from_directory.*DIST_DIR.*index.html`
   - `grep "render_template('index.html')" src/run/app.py` returns no matches (old route removed)
   - catch_all() function exists and returns index.html for non-API paths

2. **Build verification:**
   - `cd frontend-vue && npm run build` completes successfully
   - `dist/index.html` exists with hashed asset references
   - `dist/assets/` contains minified JS and CSS bundles

3. **Runtime verification (manual):**
   - Flask starts without errors: `python src/run/app.py`
   - Browser to http://localhost:5000 shows Vue.js SPA (not old template)
   - API endpoints return JSON (not HTML)
   - Non-API paths return index.html (catch-all route works)

4. **Gap closure verification:**
   - Gap 1 (duplicate root route): Old route removed, only SPA route exists
   - Gap 2 (catch-all blocked): Catch-all route now functions correctly
   - BUILD-02 requirement satisfied: Flask serves SPA from dist/ directory

5. **Requirements coverage:**
   - BUILD-01: Production build configured ✅ (from Plan 06-02)
   - BUILD-02: Build output compatible with Flask static file serving ✅ (now achievable)
   - BUILD-03: Environment variables configuration ✅ (from Plan 06-01)
   - BUILD-04: Build scripts defined ✅ (from Plan 06-01)
</verification>

<success_criteria>
- [ ] Old duplicate root route removed or commented out
- [ ] Only ONE `@app.route("/")` handler exists (serve_spa_index)
- [ ] serve_spa_index() returns send_from_directory(str(DIST_DIR), "index.html")
- [ ] Old render_template('index.html') pattern no longer in app.py
- [ ] Catch-all route verified as functional
- [ ] Flask serves Vue.js SPA at root path (manual test)
- [ ] API endpoints return JSON, not HTML (manual test)
- [ ] Non-API routes return index.html (manual test)
- [ ] BUILD-02 requirement satisfied
- [ ] Phase 6 goal achievable
</success_criteria>

<output>
After completion, create `.planning/phases/06-build-deployment/06-03-SUMMARY.md` with:
- Gap closure details (what was removed and why)
- Verification that Flask now serves SPA correctly
- Manual testing results from checkpoint
- BUILD-02 requirement now satisfied
- Phase 6 ready for final verification
</output>
