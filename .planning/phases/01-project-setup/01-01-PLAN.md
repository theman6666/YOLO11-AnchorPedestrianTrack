---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-vue/package.json
  - frontend-vue/vite.config.ts
  - frontend-vue/tsconfig.json
  - frontend-vue/tsconfig.node.json
  - frontend-vue/tsconfig.app.json
  - frontend-vue/index.html
  - frontend-vue/src/main.ts
  - frontend-vue/src/App.vue
  - frontend-vue/src/vite-env.d.ts
autonomous: true
requirements:
  - SETUP-01
user_setup: []

must_haves:
  truths:
    - "Vue 3 + Vite development server starts without errors"
    - "Project compiles TypeScript successfully"
    - "Hot module replacement works in development"
    - "Project structure follows Vue 3 official recommendations"
  artifacts:
    - path: "frontend-vue/package.json"
      provides: "Project dependencies and scripts"
      contains: '"vue": "^3.5.32", "vite": "^6.0.3"'
    - path: "frontend-vue/vite.config.ts"
      provides: "Vite build configuration"
      contains: "export default defineConfig"
    - path: "frontend-vue/tsconfig.json"
      provides: "TypeScript configuration"
      contains: "compilerOptions"
    - path: "frontend-vue/src/main.ts"
      provides: "Application entry point"
      exports: ["createApp(App)", "mount('#app')"]
    - path: "frontend-vue/src/App.vue"
      provides: "Root Vue component"
      min_lines: 10
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "npm install|npm ci"
    - from: "vite.config.ts"
      to: "src/main.ts"
      via: "Vite build pipeline"
      pattern: "plugins:.*vue\\(\\)"
    - from: "src/main.ts"
      to: "src/App.vue"
      via: "ES module import"
      pattern: "import.*from.*App.vue"
---

<objective>
Initialize Vue 3 + Vite project with TypeScript support using the official scaffold command.

Purpose: Establish modern development environment with fast HMR, TypeScript support, and official Vue 3 tooling.
Output: Working Vue 3 + Vite project in `frontend-vue/` directory with all core configuration files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-setup/01-RESEARCH.md

# Research-backed patterns from 01-RESEARCH.md

## Vue 3 Project Initialization (Pattern 1)
```bash
npm create vue@latest frontend-vue
```
Interactive prompts to accept:
- TypeScript: Yes
- JSX Support: No
- Vue Router: No (not needed for v1)
- Pinia: No (add in later phases if needed)
- Vitest: No (add in Phase 6)
- End-to-end: No
- ESLint: No (add in later phases)
- Prettier: No

## Project Structure (from RESEARCH.md)
```
frontend-vue/
├── public/
├── src/
│   ├── assets/
│   ├── components/
│   ├── App.vue
│   └── main.ts
├── index.html
├── vite.config.ts
├── package.json
└── tsconfig.json
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vue 3 + Vite project with TypeScript</name>
  <files>frontend-vue/package.json, frontend-vue/vite.config.ts, frontend-vue/tsconfig.json, frontend-vue/tsconfig.node.json, frontend-vue/tsconfig.app.json, frontend-vue/index.html, frontend-vue/src/main.ts, frontend-vue/src/App.vue, frontend-vue/src/vite-env.d.ts</files>
  <read_first>
    CLAUDE.md — Follow project's GSD workflow enforcement
  </read_first>
  <action>
    # Step 1: Create Vue 3 + Vite project
    Run the official Vue scaffold command in the project root (D:/YOLO11-AnchorPedestrianTrack/):

    ```bash
    npm create vue@latest frontend-vue
    ```

    Accept the following options when prompted:
    - Add TypeScript? → Yes
    - Add JSX Support? → No
    - Add Vue Router for Single Page Application development? → No
    - Add Pinia for state management? → No
    - Add Vitest for Unit Testing? → No
    - Add an End-to-End Testing Solution? → No
    - Add ESLint for code quality? → No
    - Add Prettier for code formatting? → No

    # Step 2: Install dependencies
    Navigate to the new project and install core dependencies:

    ```bash
    cd frontend-vue
    npm install
    ```

    # Step 3: Verify project structure
    Confirm the following files were created:
    - package.json (with vue@^3.5.32, vite@^6.0.3, typescript@^6.0.2)
    - vite.config.ts (with @vitejs/plugin-vue)
    - tsconfig.json, tsconfig.node.json, tsconfig.app.json
    - index.html
    - src/main.ts
    - src/App.vue
    - src/vite-env.d.ts

    # Step 4: Update package.json scripts (if needed)
    Verify package.json contains these scripts:
    ```json
    "scripts": {
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview"
    }
    ```

    # Step 5: Verify TypeScript configuration
    Ensure tsconfig.app.json has:
    - "target": "ES2020"
    - "module": "ESNext"
    - "moduleResolution": "bundler"
    - "jsx": "preserve"
    - "strict": true
    - "esModuleInterop": true
    - "skipLibCheck": true
    - "forceConsistentCasingInFileNames": true
  </action>
  <verify>
    <automated>cd frontend-vue && npm run dev -- --host</automated>
  </verify>
  <done>
    - Vue 3 + Vite project created in frontend-vue/ directory
    - TypeScript configured and compiling without errors
    - Development server starts on port 5173
    - Default Vue app loads in browser at http://localhost:5173
    - Hot module replacement works when editing App.vue
    - package.json contains vue@^3.5.32, vite@^6.0.3, typescript@^6.0.2
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

Run after task completion:
1. `cd frontend-vue && npm run dev -- --host` — Dev server starts without errors
2. Visit http://localhost:5173 — Default Vue app displays
3. Edit src/App.vue — HMR updates browser instantly
4. `cd frontend-vue && npm run build` — Production build succeeds
5. Check build output in frontend-vue/dist/ — Static assets generated

## Requirement Verification

- [ ] SETUP-01: Vue 3 + Vite project initialized with TypeScript support ✓
  - package.json contains "vue": "^3.5.32" (grep "package.json")
  - vite.config.ts exists and loads @vitejs/plugin-vue (grep "vite.config.ts")
  - tsconfig.json exists with strict mode enabled (grep "tsconfig.json")
  - npm run dev starts successfully (automated verification)
</verification>

<success_criteria>
## Measurable Completion

1. **Project Structure**: frontend-vue/ directory exists with all required files
2. **Dependency Installation**: npm install completed without errors
3. **Development Server**: `npm run dev` starts Vite server on port 5173
4. **TypeScript Compilation**: No TypeScript errors in console
5. **Hot Module Replacement**: Editing App.vue triggers instant browser update
6. **Production Build**: `npm run build` generates optimized dist/ directory

## Success Metrics

- Dev server starts in < 3 seconds
- HMR updates in < 200ms
- Build completes without errors
- TypeScript strict mode enabled
- All dependencies install without conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
